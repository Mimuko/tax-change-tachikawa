# Contributing

このプロジェクトへの提案・修正を歓迎します。参加により [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md) に同意したものとします。

## 進め方

1. 重複Issueがないか確認し、該当テンプレートからIssueを作成します。
2. 小さく目的の明確なブランチで変更します。
3. 定義や表示ルールを変える場合は、先に `docs/` の正本を更新します。
4. `npm test` と `npm run build` を実行します。
5. PRテンプレートに、理由・データソース・表示影響・検証結果を書きます。

## データ変更の追加要件

- 行政上の正式名称・定義・単位を保存してください。
- 原典URL、対象年度、取得日、ライセンス、加工方法を記録してください。
- 不明値を推測や0で埋めないでください。
- rawデータを追加する前に再配布可否を確認してください。
- 画面の主張がデータから直接確認できることを説明してください。

## 開発コマンド

```bash
npm ci
npm run data:fetch
npm run data:normalize
npm test
npm run build
```

MVPは立川市 × 介護です。共通化は自治体設定と処理の分離に留め、未使用の汎用フレームワークを先行追加しないでください。
